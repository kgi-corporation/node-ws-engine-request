{
  "name": "ws-engine-request",
  "version": "0.1.0",
  "description": "Connect express / restify server with engine over WebSocket connection.",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build",
    "pretest": "npm run build",
    "test": "mocha 'test/**/*.mocha.js'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kgi-corporation/node-ws-engine-request.git"
  },
  "keywords": [
    "websocket",
    "ws",
    "engine",
    "api",
    "restify",
    "express"
  ],
  "author": "Kevin Gilbert",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "chai": "^3.5.0",
    "express": "^4.14.0",
    "mocha": "^3.2.0",
    "restify": "^4.3.0",
    "supertest": "^2.0.1"
  },
  "dependencies": {
    "lodash": "^4.17.2",
    "ws": "^1.1.1"
  }
}
